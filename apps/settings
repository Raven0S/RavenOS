function drawSetting(string, x, y)
  term.setCursorPos(x,y)
  print(string)
  paintutils.drawPixel(x + string.len(string) + 1, y)
end

term.setTextColor(colors.white)
term.setBackgroundColor(colors.cyan)
print("Settings")
drawSetting("Use Rednet", 1, 3)
